<Window x:Class="MemoryGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MemoryGame.ViewModel"
        mc:Ignorable="d"
        Title="Sign In" Height="370" Width="525">
    <Window.DataContext>
        <local:SignInVM/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Welcome Enjoy this game!" HorizontalAlignment="Center" FontSize='22' FontWeight="Bold" Grid.Row="0"/>
        <UniformGrid Rows="1" Columns="2" Grid.Row="1">
            <ListBox Height="250" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser, Mode=TwoWay}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <UniformGrid Rows="2" Columns="1">
                <Image Width="100" Height="100" Source="{Binding SelectedUser.ImagePath}"/>
                <TextBox x:Name="UsernameTextBox"  Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="150" Height="20"/>
            </UniformGrid>
        </UniformGrid>
        <Grid Grid.Row="2" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <Style TargetType="Button" x:Key="ButtonSignInWindow">
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="Margin" Value="15,5,15,5"/>
                    <Setter Property="Height" Value="20"/>
                </Style>
            </Grid.Resources>
            <Button Style="{StaticResource ButtonSignInWindow}" Content="New User" Grid.Column="0" Command="{Binding Path=SignInCommands.AddCommand}"/>
            <Button Style="{StaticResource ButtonSignInWindow}" Content="Delete User" Grid.Column="1" Command="{Binding Path=SignInCommands.DeleteUserCommand}"/>
            <Button Style="{StaticResource ButtonSignInWindow}" Content="Play" Grid.Column="2" Command="{Binding Path=SignInCommands.PlayCommand}"/>
            <Button Style="{StaticResource ButtonSignInWindow}" Content="Cancel" Grid.Column="3" Command="{Binding Path=SignInCommands.CancelCommand}"/>

        </Grid>
    </Grid>
</Window>
